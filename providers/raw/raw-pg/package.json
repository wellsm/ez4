{
  "name": "@ez4/raw-pg",
  "description": "EZ4: PostgreSQL generic provider",
  "version": "0.36.0",
  "author": "Wellington Martins",
  "license": "MIT",
  "type": "module",
  "keywords": [
    "ez4"
  ],
  "repository": {
    "type": "git",
    "url": "git+https://github.com/sbalmt/ez4.git",
    "directory": "providers/raw/raw-pg"
  },
  "engines": {
    "node": ">=22.7"
  },
  "exports": {
    ".": {
      "types": "./dist/main.d.ts",
      "require": "./dist/main.cjs",
      "import": "./dist/main.mjs"
    },
    "./client": {
      "types": "./dist/client.d.ts",
      "require": "./dist/client.cjs",
      "import": "./dist/client.mjs"
    }
  },
  "sideEffects": [
    "./dist/main.cjs",
    "./dist/main.mjs"
  ],
  "workspaces": [
    "foundation/*",
    "contracts/*",
    "providers/raw/*"
  ],
  "scripts": {
    "lint": "eslint --cache",
    "clean": "rm -f *.tsbuildinfo && rm -rf dist/*",
    "build": "tsc && node tools/bundler.mjs",
    "test": "npm run build && tsc -p tsconfig.test.json && ez4 test -e ../../../local.env",
    "local:publish": "npm run build && npm run clean:registry && npm publish --registry http://localhost:4873",
    "clean:registry": "rm -rf ../../../.registry/@ez4/pg",
    "live:publish": "npm run build && npm publish --access public"
  },
  "dependencies": {
    "@ez4/database": "^0.35.0",
    "@ez4/aws-common": "^0.35.0",
    "@ez4/pgclient": "^0.35.0",
    "@ez4/pgmigration": "^0.35.0",
    "@ez4/pgsql": "^0.35.0",
    "@ez4/project": "^0.35.0",
    "@ez4/stateful": "^0.35.0",
    "@ez4/schema": "^0.35.0",
    "@ez4/utils": "^0.35.0"
  }
}